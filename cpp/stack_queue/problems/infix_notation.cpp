#include <bits/stdc++.h>

using namespace std;

int evaluate_from_prn(const string &s) {

    return 0;
}


int main() {
    istream::sync_with_stdio(false);
    cin.tie(nullptr);

    string expression;
    getline(cin, expression);
    stack<int> st;
    return 0;
}